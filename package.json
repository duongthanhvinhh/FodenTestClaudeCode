{
  "name": "playwright-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Demo Playwright + TypeScript framework using Page Object Model. Tests are generated from Markdown testcases via the generate-playwright-tests skill.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen https://www.saucedemo.com"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
